# Copy this file to .env and fill in your values.
# Never commit the real .env to version control.

# Required for preprocessing/metadata.py and preprocessing/relevance.py
OPENAI_API_KEY=sk-...

# Optional: override default data paths
# RAG_DATA_ROOT=/path/to/data
# RAG_STORAGE_ROOT=/path/to/storage
